import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // "standalone" is verwijderd — Netlify's Next.js plugin beheert de output zelf.
  // Zie: https://docs.netlify.com/frameworks/next-js/overview/
};

export default nextConfig;
